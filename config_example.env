# Prompt优化器环境配置文件
# 复制此文件为 .env 并填入实际的配置值

# ===================
# API 密钥配置
# ===================
# Google Gemini API 密钥（必需，如果使用Gemini模型）
GOOGLE_API_KEY=your_google_api_key_here

# OpenAI API 密钥（可选，如果使用OpenAI模型）
OPENAI_API_KEY=your_openai_api_key_here

# ===================
# 代理配置
# ===================
# 是否启用默认代理（true/false，默认：true）
ENABLE_DEFAULT_PROXY=true

# HTTPS 代理（可选，默认使用 http://127.0.0.1:7890）
HTTPS_PROXY=http://127.0.0.1:7890

# HTTP 代理（可选，默认使用 http://127.0.0.1:7890）
HTTP_PROXY=http://127.0.0.1:7890

# ===================
# 服务器配置
# ===================
# 服务器主机地址（默认：0.0.0.0）
SERVER_HOST=0.0.0.0

# 服务器端口（默认：9999）
SERVER_PORT=9999

# 日志级别（debug/info/warning/error，默认：info）
LOG_LEVEL=info

# 工作进程数（默认：1）
WORKERS=1

# 是否启用热重载（true/false，默认：false）
RELOAD=false

# ===================
# 模型配置
# ===================
# 默认模型类型（gemini/openai，默认：openai）
DEFAULT_MODEL_TYPE=openai

# 模型温度参数（0.0-1.0，默认：0.7）
MODEL_TEMPERATURE=0.7

# 请求超时时间（秒，默认：60）
REQUEST_TIMEOUT=60

# 最大重试次数（默认：3）
MAX_RETRIES=3

# ===================
# Web界面配置
# ===================
# Web界面端口（默认：7860）
WEB_PORT=7860

# Web界面主机（默认：0.0.0.0）
WEB_HOST=0.0.0.0

# ===================
# 调试和监控
# ===================
# 是否启用详细日志（true/false，默认：false）
VERBOSE_LOGGING=false

# 是否启用性能监控（true/false，默认：false）
ENABLE_MONITORING=false

# 日志文件路径（默认：prompt_optimizer.log）
LOG_FILE_PATH=prompt_optimizer.log

# ===================
# 示例配置
# ===================
# 以下是一些常用配置示例：

# 开发环境配置示例：
# RELOAD=true
# LOG_LEVEL=debug
# VERBOSE_LOGGING=true

# 生产环境配置示例：
# WORKERS=4
# LOG_LEVEL=warning
# ENABLE_MONITORING=true

# 代理环境配置示例：
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890

# 多模型配置示例：
# DEFAULT_MODEL_TYPE=gemini
# MODEL_TEMPERATURE=0.5
# REQUEST_TIMEOUT=120 